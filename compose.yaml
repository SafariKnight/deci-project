services:
  postgresql:
    image: postgres:18.4
    restart: always
    env_file: .env
    ports:
      - 8081:5432
    volumes:
      - postgres_data:/var/lib/postgresql/

volumes:
  postgres_data:
